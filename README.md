# Pinch interaction prototype

> Throwaway prototype. This is not production application code.

Run on macOS 26 with:

```sh
swift run
```

Compare the three variants with the bottom switcher. Click or hover over the
pinch marker at the right edge of the composer, then choose a phrase. The
switches simulate a missing target and reduced motion.
